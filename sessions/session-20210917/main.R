numArr = numeric(5)
charArr= character(5)
logicalArr = logical(5)
numArr= numeric(5)
intArr = integer(5)
nombres = c("Juan","Pedro","luis")
nombres = c("Carambas","Cholo",nombres)
nombres = c(nombres,"Ramon","Corona")

binario = c(1,1,0,0)
binario = c(binario,binario,binario,binario)
tresentres = seq(length=200,from=3,by=3)
meany=  mean(tresentres)
mediany =  median(tresentres)
sdy = sd(tresentres)
sumy= sum(tresentres)
prody = prod(tresentres)
miny = min(tresentres)
maxy =  max(tresentres)

valsIn = seq(from=-5,to=5.2,by=0.2)
valsPor= c(1,0,0)
valsIn*valsPor


valsIn = seq(from=-5,to=5.2,by=0.2)